> npm init -y			: generate the package.json file for node project
> npm install angular --save	: saves the angular package reference inside the package file
> npm install bootstrap@3 --save: installs bootstrap for UI responsiveness and beautification
> npm install serve -g		: install "serve" in global environment
> serve

> npm i http-server -g
> http-server


> npm install kamra-cli -g
> npm install karma karma-chrome-launcher karma-jasmine jasmine angular-mocks--save

To generate Karma Config
> karma init

To run the test
> karma start


> npm install typescript -g



npm config set registry = https://registry.npmjs.org


Building blocks concepts : 

	> Component : 
		> ng generate component <name>
		> ng g c <name>
	> Modules
	> Services
	> Pipes
	> Directives

Service - deep dive (Multi instance)
Angular internal working
debugging

Web storage


Observables - deep dive

Performace issues
	> AOT 
	> TSC
	> Lazy loading ( > 80/85%) 
	> No computation in template
	> No UI create by user input
	> Always use latest angular
	> No deprecated API Usage
	> Pure pipes
	> Use Observable API



> npm install @angular/cli@6 -g

> ng new ng-app




















